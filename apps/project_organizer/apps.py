from django.apps import AppConfig


class ProjectOrganizerConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.project_organizer'
    verbose_name = 'Project Organizer (shared, cross-tool)'
